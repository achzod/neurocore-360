# 📋 INSTRUCTIONS COMPLÈTES - NEUROCORE 360

## 📁 Structure du dossier docs/

Ce dossier contient toute la documentation nécessaire pour comprendre et implémenter les fonctionnalités de NEUROCORE 360.

### Fichiers inclus :

1. **INSTRUCTIONS.md** (ce fichier) - Instructions générales
2. **LOGO_URLS.md** - URLs et ressources pour télécharger les logos officiels
3. **INTEGRATION_REVIEW_FORM.md** - Guide d'intégration du formulaire d'avis

---

## 🎯 CONTEXTE PROJET

**NEUROCORE 360** est une plateforme d'audit métabolique premium qui génère des rapports personnalisés via Gemini AI.

### Stack technique :
- **Frontend**: React + TypeScript
- **Backend**: Node.js + Express + TypeScript
- **AI**: Google Gemini 3 Pro
- **Style**: CSS custom (fond sombre #1a1a2e)

---

## ✅ TÂCHES À RÉALISER

### 1. Télécharger les logos officiels

📄 **Fichier**: `LOGO_URLS.md`

**Action**: 
- Télécharger tous les logos listés depuis les sites officiels
- Placer les logos dans `/public/logos/` avec les noms exacts mentionnés
- Formats recommandés: PNG avec fond transparent
- Dimensions: 200-300px de largeur

**Logos requis**:
- Section Médias: Bloomberg, Google News, Financial Post, AP, Dow Jones, canoe.com, Apple News
- Section Certifications: ISSA, NASM, Precision Nutrition, Pre-Script, Fitness Australia, AFAA, ACE, EuropeActive, UK REPs

---

### 2. Intégrer le formulaire d'avis dans la page de rapport

📄 **Fichier**: `INTEGRATION_REVIEW_FORM.md`

**Action**:
- Importer le composant `ReviewForm` dans la page d'affichage du rapport
- Passer l'`auditId` au composant
- Créer la fonction `handleReviewSubmit` qui appelle `/api/submit-review`
- Placer le composant à la fin du rapport, juste avant le footer

**Code exemple fourni dans le fichier**.

---

### 3. Créer le système de base de données pour les avis

**Tables nécessaires**:

```sql
CREATE TABLE reviews (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  audit_id VARCHAR(255) NOT NULL,
  user_id VARCHAR(255),
  rating INTEGER NOT NULL CHECK (rating >= 1 AND rating <= 5),
  comment TEXT NOT NULL,
  status VARCHAR(20) DEFAULT 'pending' CHECK (status IN ('pending', 'approved', 'rejected')),
  created_at TIMESTAMP DEFAULT NOW(),
  reviewed_at TIMESTAMP,
  reviewed_by VARCHAR(255)
);

CREATE INDEX idx_reviews_audit_id ON reviews(audit_id);
CREATE INDEX idx_reviews_status ON reviews(status);
```

**Mettre à jour** `server/index.ts`:
- Dans `POST /api/submit-review`: Sauvegarder dans la DB avec `status = 'pending'`
- Dans `GET /api/reviews`: Récupérer uniquement les avis avec `status = 'approved'`

---

### 4. Créer le dashboard admin pour valider les avis

**Endpoints à créer**:

```typescript
// GET /api/admin/reviews/pending
// Récupérer tous les avis en attente de validation

// POST /api/admin/reviews/:reviewId/approve
// Approuver un avis (status = 'approved')

// POST /api/admin/reviews/:reviewId/reject
// Rejeter un avis (status = 'rejected')
```

**Interface admin à créer**:
- Liste des avis en attente (`status = 'pending'`)
- Affichage: note, commentaire, date, audit ID
- Boutons "Approuver" / "Rejeter"
- Option de modifier le commentaire si nécessaire
- Afficher l'avis après validation/rejet

---

## 📂 Structure des fichiers importants

```
neurocore/
├── client/
│   └── src/
│       ├── pages/
│       │   └── Home.tsx              # Page d'accueil avec carousels + reviews
│       ├── components/
│       │   └── ReviewForm.tsx        # Formulaire d'avis
│       └── styles/
│           ├── Home.css              # Styles page d'accueil
│           └── ReviewForm.css        # Styles formulaire
├── server/
│   ├── index.ts                      # API endpoints (déjà mis à jour)
│   ├── geminiPremiumEngine.ts        # Génération audit
│   ├── supplementEngine.ts           # Stack de suppléments
│   └── cta.ts                        # Templates CTAs
└── public/
    └── logos/                        # ⚠️ À CRÉER - Logos officiels ici
        ├── bloomberg-logo.png
        ├── google-news-logo.png
        ├── issa-logo.png
        └── ... (voir LOGO_URLS.md)
```

---

## 🔗 Endpoints API disponibles

- `GET /api/health` - Health check
- `POST /api/generate-premium-audit` - Générer un audit
- `POST /api/submit-review` - Soumettre un avis (✅ déjà créé)
- `GET /api/reviews` - Récupérer avis validés (✅ déjà créé)
- `GET /api/admin/reviews/pending` - ⚠️ À CRÉER
- `POST /api/admin/reviews/:reviewId/approve` - ⚠️ À CRÉER
- `POST /api/admin/reviews/:reviewId/reject` - ⚠️ À CRÉER

---

## 📝 Notes importantes

1. **Logos**: Tous les logos doivent être officiels et respecter les droits d'utilisation
2. **Avis**: Les avis sont modérés (status 'pending' → 'approved'/'rejected' par admin)
3. **Reviews section**: Les 14 reviews affichées sur la page Home sont des "fausses" reviews de beta testers (score moyen 4.9/5)
4. **ReviewForm**: Seuls les avis soumis via le formulaire et validés par l'admin apparaîtront dans la section reviews (après intégration DB)

---

## 🚀 Ordre d'implémentation recommandé

1. ✅ Télécharger les logos → `/public/logos/`
2. ✅ Intégrer ReviewForm dans la page de rapport
3. ⚠️ Créer la table `reviews` dans la DB
4. ⚠️ Mettre à jour les endpoints avec la DB
5. ⚠️ Créer le dashboard admin pour valider les avis
6. ⚠️ Tester le flux complet: soumission → validation → affichage

---

**Dernière mise à jour**: 2025-12-31

