# 📖 LIRE EN PREMIER - Guide pour l'Agent IA

## 🎯 TU DOIS GÉNÉRER UN AUDIT PREMIUM

Tu dois utiliser le système existant pour générer un audit complet. **NE PAS créer de nouveau code.**

## 🚀 PAR OÙ COMMENCER ?

### 1️⃣ Lire ce guide (5 minutes)
👉 `docs/DEMARRAGE_RAPIDE.md` - Guide ultra simple en 3 étapes

### 2️⃣ Lire les instructions strictes (10 minutes)
👉 `docs/INSTRUCTIONS_STRICTES.md` - **OBLIGATOIRE** - Liste de ce qu'il NE faut PAS faire

### 3️⃣ Comprendre le système (15 minutes)
👉 `docs/UTILISER_GEMINI_PREMIUM_ENGINE.md` - Comment fonctionne le système

### 4️⃣ Vérifier les fichiers (5 minutes)
👉 `docs/FICHIERS_ESSENTIELS.md` - Liste des fichiers nécessaires

## ⚠️ AVANT DE MODIFIER QUOI QUE CE SOIT

**TU DOIS LIRE :**
1. ✅ `server/geminiPremiumEngine.ts` (lignes 116-391 pour voir les prompts)
2. ✅ `server/config.ts` (pour voir la configuration)
3. ✅ `docs/INSTRUCTIONS_STRICTES.md` (pour voir les interdictions)

## 🚫 NE JAMAIS FAIRE (sous peine d'erreur)

1. ❌ Créer de nouveaux fichiers de prompts (ils existent déjà)
2. ❌ Utiliser `getSectionPromptData` (fonction inexistante)
3. ❌ Modifier les 18 sections (elles sont complètes)
4. ❌ Changer le modèle Gemini (`gemini-3-pro-preview` est le bon)
5. ❌ Créer un système parallèle (le système existe déjà)

## ✅ CE QU'IL FAUT FAIRE

```typescript
// 1. Importer
import { generateAndConvertAudit } from './server/geminiPremiumEngine';

// 2. Appeler
const result = await generateAndConvertAudit(clientData, photoAnalysis, 'PREMIUM');

// 3. Utiliser
if (result.success) {
  console.log(result.txt); // Rapport complet
}
```

## 📋 CHECKLIST AVANT DE COMMENCER

- [ ] J'ai lu `docs/DEMARRAGE_RAPIDE.md`
- [ ] J'ai lu `docs/INSTRUCTIONS_STRICTES.md`
- [ ] J'ai lu `server/geminiPremiumEngine.ts` (au moins les sections)
- [ ] Je comprends que TOUS les prompts sont déjà dans le code
- [ ] Je ne vais PAS créer de nouveaux fichiers
- [ ] Je vais utiliser `generateAndConvertAudit()` uniquement

## 🆘 SI QUELQUE CHOSE NE FONCTIONNE PAS

1. **Relire `docs/INSTRUCTIONS_STRICTES.md`** - La plupart des erreurs y sont listées
2. **Vérifier `server/config.ts`** - Le modèle doit être `gemini-3-pro-preview`
3. **Vérifier `server/geminiPremiumEngine.ts`** - Les 18 sections doivent être présentes
4. **Ne pas créer de code "de secours"** - Le système fonctionne, il faut juste l'utiliser correctement

## 📁 Structure des docs

```
docs/
├── LIRE_EN_PREMIER.md          ← TU ES ICI
├── DEMARRAGE_RAPIDE.md         ← Commence par là
├── INSTRUCTIONS_STRICTES.md    ← Règles à respecter (IMPORTANT)
├── UTILISER_GEMINI_PREMIUM_ENGINE.md  ← Comprendre le système
└── FICHIERS_ESSENTIELS.md      ← Liste des fichiers nécessaires
```

## 🎯 RÉSUMÉ EN 1 PHRASE

**Utiliser `generateAndConvertAudit()` de `geminiPremiumEngine.ts` - tout est déjà configuré, ne rien créer.**

---

**Lis `DEMARRAGE_RAPIDE.md` maintenant 👉**

